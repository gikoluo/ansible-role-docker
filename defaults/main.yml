---
# defaults file for avinetworks.docker


docker_cfg: daemon.json.j2
docker_cfg_dest: /etc/docker/daemon.json

docker_group_members: []

daemon_settings: 
  hosts:
    - "unix:///var/run/docker.sock"

  # registry-mirrors:
  #   - "http://99c6e2d5.m.daocloud.io"
  # hosts:
  #   - "tcp://0.0.0.0:2375"
  #   - "unix:///var/run/docker.sock"

